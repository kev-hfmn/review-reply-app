import { metadata as updatePasswordMetadata } from './metadata';

export const metadata = updatePasswordMetadata;

export default function UpdatePasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}