import { metadata as resetPasswordMetadata } from './metadata';

export const metadata = resetPasswordMetadata;

export default function ResetPasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}